<!DOCTYPE html>
<html>
  <head>
    <title>Yaybtc</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if @user && !@user.auto_buy_sell_enabled? %>
      <div class="auto-buy-sell-disabled">
        Auto Buy/Sell is disabled for this account. You can add BTC to the wallet but it will not execute any buy/sell orders. <strong><%= link_to("Click here to enable it.", accounts_toggle_path, method: :post, class: "toggle-link") %></strong>
      </div>

    <% else %>
      <div class="auto-buy-sell-enabled">
        Auto Buy/Sell is enabled for this account. This app will execute buy/sell orders based on current trends. <strong><%= link_to("Click here to disable it.", accounts_toggle_path, method: :post, class: "toggle-link") %></strong>
      </div>
    <% end %>

    <br />

    <div class="content">
      <ul class="nav">
        <li><%= link_to("LOGO", root_path) %></li>
        <% unless user_signed_in? %>
          <li><%= link_to("Sign In", new_user_session_path) %></li>
          <li><%= link_to("Sign Up", new_user_registration_path) %></li>
        <% else %>
          <li><%= link_to("Logout", destroy_user_session_path, method: :delete) %></li>
        <% end %>
      </ul>
      <%= yield %>

      <hr />
    </div>

    <div class="footer">
      Data Provided by <a href="https://winkdex.com/" target="_blank">WinkDex</a> -
      API provided by <a href="https://coinbase.com/" target="_blank">CoinBase</a> -
      Copyright YayBTC <%= Time.now.year %>
    </div>
  </body>
</html>
